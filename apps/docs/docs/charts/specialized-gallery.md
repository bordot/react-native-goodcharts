# Specialized Gallery

The current scaffold also includes dedicated implementations for:

- `HeatmapChart`
- `ProgressBarChart`
- `SparklineChart`

These charts are intentionally narrow and optimized for common dashboard cases where a full cartesian or radial composition would be too heavy.
