{
  "name": "react-native-goodcharts-workspace",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.9.2",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm run build --workspace react-native-goodcharts",
    "type-check": "npm run type-check --workspace react-native-goodcharts",
    "lint": "biome check .",
    "test": "jest --runInBand",
    "bench": "tsx benchmarks/render-time.ts",
    "release:version": "changeset version",
    "release:publish": "changeset publish",
    "pack:dry-run": "npm run build --workspace react-native-goodcharts && npm pack --dry-run --ignore-scripts --workspace react-native-goodcharts",
    "docs:start": "npm run start --workspace docs",
    "example:start": "npm run start --workspace example"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@changesets/cli": "2.29.2",
    "@types/d3-array": "^3.2.2",
    "@types/d3-scale": "^4.0.9",
    "@types/jest": "29.5.14",
    "@types/react": "18.3.12",
    "@types/react-native": "0.73.0",
    "jest": "29.7.0",
    "react-test-renderer": "18.3.1",
    "ts-jest": "29.2.6",
    "tsx": "4.19.3",
    "typescript": "5.8.2"
  }
}
